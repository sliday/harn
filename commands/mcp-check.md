---
description: "Generate MCP server health check hook"
allowed-tools: "Read,Write,Edit,Bash,Glob"
---

Use the `harn:mcp-validator` skill to generate a health check that validates MCP servers are running before the agent starts work.
